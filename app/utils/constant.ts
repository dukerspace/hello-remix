export enum CategoryType {
  INCOME = 'Income',
  EXPENSE = 'Expense',
  TRANSFER = 'Transfer'
}

export enum AuthStatus {
  authen = 'authenticated',
  loading = 'loading',
  unauthen = 'unauthenticated'
}
